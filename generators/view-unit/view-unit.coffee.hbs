require 'views/{{#camelize}}{{name}}{{/camelize}}'

{{#camelize}}{{name}}{{/camelize}}View = moduleLibrary.get '{{#camelize}}{{name}}{{/camelize}}.View'

describe 'View {{#camelize}}{{name}}{{/camelize}}', ->
  beforeEach ->
    @{{name}}View = {{#camelize}}{{name}}{{/camelize}}View.create()

  afterEach ->
    @{{name}}View.dispose()
